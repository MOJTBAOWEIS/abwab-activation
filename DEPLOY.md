# النشر — خطوة بخطوة

كل شي بالكود جاهز ومرفوع في git. الباقي يحتاج حسابك أنت.

المدة المتوقعة: **١٥ دقيقة**.

---

## القيم الجاهزة — انسخها الآن

هذه مولّدة لك خصيصاً. احفظها بمكان آمن.

```
ABWAB_SECRET_KEY = b51bd8717a627109f851bd52eef8649f45c8e0ab0df0d06437c0bad5bbe063ba
ABWAB_ENV        = production
ABWAB_DB         = /data/abwab.db
```

وكلمة سر المدير — اختر وحدة **٨ خانات على الأقل، حروف وأرقام**، مثلاً:

```
ABWAB_MANAGER_PASSWORD = ghusuq2026abwab
```

> غيّرها لشي تحفظه أنت. هذه هي الوحيدة اللي تدخل بيها أول مرة.

---

## ١. GitHub

أنشئ حساب على [github.com](https://github.com) إذا ما عندك، بعدين:

**New repository** → الاسم `abwab-activation` → اختر **Private** → **Create**.

بعدها نفّذ هذول بمجلد `D:\Abwaab` (بدّل `USERNAME` باسمك):

```bash
git remote add origin https://github.com/USERNAME/abwab-activation.git
```

```bash
git branch -M main && git push -u origin main
```

راح يطلب تسجيل دخول — استخدم نافذة المتصفح اللي تفتح.

> الكود محضّر بحيث **ما فيه ولا كلمة سر ولا بيانات زبائن**. تأكدت من هذا قبل الرفع. مع ذلك خلّي المستودع Private.

---

## ٢. Railway

[railway.app](https://railway.app) → **Login with GitHub**.

**New Project** → **Deploy from GitHub repo** → اختر `abwab-activation`.

راح يبدأ يبني لحاله. انتظره يخلص.

---

## ٣. القرص الدائم — لا تتخطّاها

داخل الخدمة: **Settings** → **Volumes** → **New Volume**

Mount path:

```
/data
```

> بدونها، **كل الليدات تنمسح** أول ما تنشر أي تحديث. بصمت، بدون أي رسالة خطأ.
> هذي أكثر طريقة يضيع بيها مشروع مثل هذا أسبوع شغل ميداني.

---

## ٤. المتغيرات

**Variables** → أضف الأربعة:

| الاسم | القيمة |
|---|---|
| `ABWAB_SECRET_KEY` | القيمة الطويلة فوق |
| `ABWAB_ENV` | `production` |
| `ABWAB_DB` | `/data/abwab.db` |
| `ABWAB_MANAGER_PASSWORD` | كلمة سر المدير اللي اخترتها |

بعد الحفظ راح يعيد النشر لحاله.

---

## ٥. الرابط

**Settings** → **Networking** → **Generate Domain**

راح تحصل على شي مثل `abwab-activation-production.up.railway.app`.

---

## ٦. كلمات سر المروّجين

**Deployments** → **View Logs** — دوّر على هذا، يظهر **مرة وحدة فقط**:

```
==========================================================
FIRST-RUN PASSWORDS — copy these now, they are not shown again
==========================================================
   P01    p01d1bvug4d
   P02    p028o5odnme
   ...
```

انسخها كلها الآن. إذا ضاعت، تقدر تعطي كل واحد كلمة سر جديدة من **الإعدادات**.

---

## ٧. الفحص

بمجلد `D:\Abwaab`:

```bash
python deploy_check.py https://your-app.up.railway.app
```

يتأكد من HTTPS، ومن أن الخادم يرد، والأهم: **أن اللوحة والـ API مقفولة فعلاً**
على أي شخص غير مسجّل.

---

## ٨. الإطلاق

1. افتح الرابط، سجّل دخول كـ **مدير المشروع** بكلمة السر اللي اخترتها
2. **الإعدادات** → راجع الفروع والمروّجين والشفتات
3. غيّر كلمات سر المروّجين إذا تحب شي أسهل يحفظونه
4. **جدول المناوبات** → جدول أول أيام
5. أعط كل مروّج الرابط وكلمة سره

**مهم:** خلّي كل مروّج يسوي **«إضافة إلى الشاشة الرئيسية»** بالموبايل قدامك.
بعدها يفتح مثل التطبيق، مباشرة على شاشة الدخول.

---

## ٩. قبل أول شفت — الاختبار الوحيد اللي ما ينفع تتخطّاه

روح للفرع، **قف بمكان البوث بالضبط**، وافتح الموقع من موبايل مروّج.

الاستضافة تحل الوصول، **مو التغطية**. داخل السوبرماركت غالباً ما توجد بيانات
موبايل. إذا ما اشتغل هناك، الحل واي فاي الفرع أو راوتر رخيص — مو استضافة ثانية.

---

## النسخ الاحتياطي

القرص الدائم **ليس نسخة احتياطية**. مرة بالأسبوع:

لوحة المدير → **تصدير CSV** → احفظ الملف.

دقيقتين، وتضمن أنك ما تخسر شي حتى لو راح كل شي.
